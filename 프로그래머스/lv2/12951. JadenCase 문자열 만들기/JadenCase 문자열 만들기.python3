def solution(s):
    word = list(s.split(" "))
    result = []
    for i in word:
        if i != "":
            i = i.lower()
            h = i[0].upper() + i[1:]
            result.append(h)
        else:
            result.append(i)
    return " ".join(result)